****
THE SCSS STRUCTURE HERE IS ORGANIZED TO SEPARATE CORE STYLES, TEMPLATE STRUCTURE, UNIQUE MODULES and CUSTOM CLIENT STYLES.
****


* This file may not actually be in use f using a CHILD theme (which you should be doing). There is a DEQUEUE function in the child theme that will unhook this file and use the child verion instead.

* _BASE folder
The contents in this folder are meant to be interchangeable / overwritten. These willmost likely be updated in each new CHILD theme. Do best to keep track of what folder is most current, but they should never be intentionally unique.