These JSON files are automatically saved when you update a "field group" within ACF.

Reference: https://www.advancedcustomfields.com/resources/local-json/

Using this feature greatly reduces the number of database queries required and will help to speed up the site. Since ACF can pile up the queries this is an important feature to keep activated.