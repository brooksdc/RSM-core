These are Advanced Custom Fields (ACF) Export Files.

Use them to quickly import the default field groups into your WP install. We do it this way so custom fields can be modified or added to within the admin after installing this default set, instead of locking the fields into the plugin files.